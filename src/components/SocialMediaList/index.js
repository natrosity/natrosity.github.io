import { css } from "@emotion/react";
import * as React from "react";

import SocialMediaLink from "natrosity-components/SocialMediaLink";

const SOCIAL_MEDIA_LINKS = {
  instagram: "",
  twitter: "",
  facebook: "",
  youtube: "",
};

const SocialMediaList = (props) => {
  return (
    <div css={css``}>
      {/* <SocialMediaLink imageLink="instagram/icon.png" /> Twitter. Facebook. */}
      YouTube
    </div>
  );
};

export default SocialMediaList;
